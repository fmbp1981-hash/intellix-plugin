{
  "id": "TASK-002",
  "title": "Distribute a pinned kernel and generate vendor adapters",
  "status": "DRAFT",
  "objective": "Make client projects consume a versioned kernel snapshot and generated adapters without manual policy duplication.",
  "source": {"prd": "README.md", "architecture": "docs/architecture/INTELLIX-TARGET-ARCHITECTURE.md", "spec": "docs/architecture/AUTHORITY-AND-TRUST-MODEL.md", "adrs": ["docs/adr/0001-vendor-neutral-multiagent-kernel.md"]},
  "risk": {"level": "high", "reasons": ["Changes project bootstrap and framework distribution"], "irreversible_actions": []},
  "ownership": {"architect": "claude", "executor": "codex", "reviewer": "claude-independent", "domain_role": "platform-engineer"},
  "scope": {
    "create": ["framework/sync.py", "framework/generate_adapters.py", "framework/tests/test_distribution.py", "framework/schemas/lock.schema.json"],
    "modify": ["framework/README.md", "framework/templates/PROJECT.yaml", "framework/adapters/**", "intellix-templates/root-template/**", "skills/project-kickoff/SKILL.md", "commands/new-project.md", "tasks/TASK-002.yaml"],
    "forbidden": [".env*", "**/.env*", "issues/**", "infra/production/**", "global-config/settings.json"]
  },
  "dependencies": ["TASK-001"],
  "acceptance_criteria": [
    {"id": "AC-001", "given": "a fresh client project", "when": "the supported sync/bootstrap runs", "then": "it receives a kernel version and digest that validation can resolve locally"},
    {"id": "AC-002", "given": "canonical framework and project configuration", "when": "adapters are generated", "then": "AGENTS and CLAUDE entry points contain links and local operations without duplicating policies"},
    {"id": "AC-003", "given": "a manually modified or stale generated snapshot", "when": "validation runs", "then": "digest/version drift blocks the project"},
    {"id": "AC-004", "given": "a live Claude installation", "when": "sync is requested", "then": "the direction is repository to installation and no reverse mutation is implicit"}
  ],
  "verification": [
    {"command": "python3 framework/validate.py --root . --all", "proves": "Distribution metadata remains valid"},
    {"command": "python3 -m unittest discover -s framework/tests -v", "proves": "Fresh, stale and tampered distribution paths are covered"},
    {"command": "bash hooks/scripts/validate-hooks.sh .", "proves": "Claude adapter hook wiring remains compatible"}
  ],
  "gates": ["schema", "fileset", "lint", "tests", "security_review", "independent_review", "rollback_plan", "human_approval", "worktree"],
  "rollback": "Revert the distribution implementation commit; existing projects remain pinned to their previous framework version.",
  "notes": ["Keep the kernel in this repository for MVP while making it extraction-ready.", "Generated project copies are deliberate pinned artifacts, not competing sources."],
  "handoff": {"required_evidence": ["fresh-project fixture", "digest drift test", "generated adapter diff", "independent review"], "open_questions": []}
}
