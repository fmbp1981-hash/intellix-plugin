{
  "id": "TASK-002",
  "title": "Distribute a pinned kernel and generate vendor adapters",
  "status": "APPROVED",
  "objective": "Make client projects consume a versioned kernel snapshot and generated adapters without manual policy duplication.",
  "source": {"prd": "README.md", "architecture": "docs/architecture/INTELLIX-TARGET-ARCHITECTURE.md", "spec": "docs/architecture/AUTHORITY-AND-TRUST-MODEL.md", "adrs": ["docs/adr/0001-vendor-neutral-multiagent-kernel.md"]},
  "risk": {"level": "high", "reasons": ["Changes project bootstrap and framework distribution"], "irreversible_actions": []},
  "ownership": {"architect": "claude", "executor": "codex", "reviewer": "claude-independent", "domain_role": "platform-engineer"},
  "scope": {
    "create": ["framework/sync.py", "framework/generate_adapters.py", "framework/tests/test_distribution.py", "framework/schemas/lock.schema.json", "intellix.lock.json"],
    "modify": ["framework/framework.yaml", "framework/validate.py", "framework/schemas/project.schema.json", "framework/tests/test_validate.py", "framework/tests/fixtures/projects/valid/intellix.yaml", "framework/README.md", "framework/templates/PROJECT.yaml", "framework/adapters/**", "intellix.yaml", "intellix-templates/root-template/**", "skills/project-kickoff/SKILL.md", "commands/new-project.md", "tasks/TASK-002.yaml"],
    "forbidden": [".env*", "**/.env*", "issues/**", "infra/production/**", "global-config/settings.json"]
  },
  "dependencies": ["TASK-001"],
  "acceptance_criteria": [
    {"id": "AC-001", "given": "a fresh client project", "when": "the supported sync/bootstrap runs", "then": "it receives a kernel version and digest that validation can resolve locally"},
    {"id": "AC-002", "given": "canonical framework and project configuration", "when": "adapters are generated", "then": "AGENTS and CLAUDE entry points contain links and local operations without duplicating policies"},
    {"id": "AC-003", "given": "a manually modified or stale generated snapshot", "when": "validation runs", "then": "digest/version drift blocks the project"},
    {"id": "AC-004", "given": "a live Claude installation", "when": "sync is requested", "then": "the direction is repository to installation and no reverse mutation is implicit"}
  ],
  "verification": [
    {"command": "python3 framework/validate.py --root . --all", "proves": "Distribution metadata remains valid"},
    {"command": "python3 -m unittest discover -s framework/tests -v", "proves": "Fresh, stale and tampered distribution paths are covered"},
    {"command": "bash hooks/scripts/validate-hooks.sh .", "proves": "Claude adapter hook wiring remains compatible"}
  ],
  "gates": ["schema", "fileset", "lint", "tests", "security_review", "independent_review", "rollback_plan", "human_approval", "worktree"],
  "rollback": "Revert the distribution implementation commit; existing projects remain pinned to their previous framework version.",
  "notes": ["Keep the kernel in this repository for MVP while making it extraction-ready.", "Generated project copies are deliberate pinned artifacts, not competing sources.", "The controlling human approved the expanded fileset and read-only Claude architecture review on 2026-09-17.", "Claude architecture review confirmed the lock design after TASK-001 released completed fileset ownership; no unresolved architectural choice remains.", "Threat model: project paths, lock content and local snapshots are untrusted. Reuse fail-closed root containment, forbid traversal and symlink escape, fix the digest algorithm to SHA-256, compare version independently, stage writes before replacement, and never read a consumer snapshot as repository input.", "The digest covers the sorted normative kernel manifest: framework.yaml plus its declared policies, schemas and roles. Control-plane code and generated adapters remain outside the normative kernel digest.", "A copied local validator is an advisory consistency tool, not a tamper-proof trust anchor. Authoritative evidence runs an intact reviewed source or exact CI revision against the explicit consumer root.", "Independent Claude review returned APPROVE conditional on documenting that trust boundary; the condition and both low-severity hardening suggestions were addressed.", "Architecture documents remain marked Proposed in-tree because this task was not authorized to change their status; controlling-session human approvals are recorded without falsifying document provenance.", "The controlling human explicitly approved the TASK-002 handoff on 2026-09-17 after exact-revision CI, GitGuardian and independent review passed."],
  "handoff": {"required_evidence": ["fresh external project receives a locally resolvable lock and validates", "35 passing tests covering missing, stale, tampered, escaped and unknown-digest paths", "deterministic lock equality across identical snapshots", "source repository remains byte-identical after consumer resync", "generated adapters are short links and refuse unmarked overwrite", "explicit-root validation, sync check, compileall, hook validation and diff-check output", "changed-file inventory restricted to the approved fileset", "independent Claude review decision: APPROVE with documented trust boundary"], "open_questions": []}
}
