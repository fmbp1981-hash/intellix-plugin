# Prompt: {{agent_name}}

> Generated by IntelliX Agent Creation Engine v2
> Version: {{version}} | Domain: {{business_domain}}

---

## System Prompt (Complete)

```
{{system_prompt_full_text}}
```

---

## System Prompt Sections (Modular)

Use these sections if your implementation supports modular prompt composition. Sections are ordered by priority (lower number = earlier in prompt).

### Section: {{section_id}} (Priority: {{priority}})
**Condition:** {{condition_or_always}}

```
{{section_content}}
```

*Repeat for each section.*

---

## User Prompt Template

This template wraps each user message with context before sending to the LLM.
Use `{{placeholder}}` syntax — replace with actual values at runtime.

```
{{user_prompt_template}}
```

---

## Few-Shot Examples

These examples train the LLM on ideal behavior for this agent.

### Example {{n}}
**Context:** {{example_context}}

**User:**
```
{{example_user_message}}
```

**Assistant:**
```
{{example_assistant_response}}
```

*Repeat for each example.*

---

## Output Format

**Type:** {{output_format_type}}
**Instructions:** {{output_format_instructions}}

{{if output_format_type == "json"}}
**Expected JSON Schema:**
```json
{{output_format_schema}}
```
{{/if}}

---

## Guardrails

| Rule | Severity | Action on Violation |
|---|---|---|
| {{guardrail_rule}} | {{severity}} | {{action_on_violation}} |

---

## Notes

- The system_prompt is the primary artifact — it defines agent behavior
- Modular sections allow dynamic composition (e.g. include patient_context only when patient is identified)
- Few-shot examples should be updated as the agent evolves
- Guardrails with severity "hard_block" must be enforced at the application level, not just in the prompt
