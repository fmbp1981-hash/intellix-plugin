# Project agent contract

Read `intellix.yaml`, the canonical framework it references, relevant ADRs and
the assigned `tasks/TASK-NNN.yaml` before editing. Work only inside the fileset,
run every verification command and produce a structured handoff. Executor and
reviewer must be independent; CI failures and human approval gates are binding.
