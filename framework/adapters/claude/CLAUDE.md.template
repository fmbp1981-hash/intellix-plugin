<!-- Generated by IntelliX; edit the source template, not this file. -->
# Claude Code project adapter

Read `AGENTS.md`, `intellix.yaml`, `{{framework_source}}` and the assigned
`tasks/TASK-NNN.yaml`. Claude is the project architect and independent reviewer
unless the Task Contract explicitly assigns another adapter.

Local validation: `python3 framework/validate.py --root . --all`.
